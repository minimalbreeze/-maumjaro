# 개발자에게 한마디 — 받는 곳 만들기 (5분)

앱 설정과 기록 탭에 **💬 개발자에게 한마디**가 생겼습니다. 한 번 보내고 끝이 아니라
**SNS 댓글처럼 계속 이어지는 1:1 대화**입니다.

```
사용자가 앱에서 한마디
      ↓
운영자 화면(/admin)에 쌓임 — "답장 차례" 표시
      ↓  (직접 들어가서 답장을 씀)
그 사람 앱에 말풍선 + 빨간 점 + "개발자 답장이 도착했어요"
      ↓
사용자가 또 답함 → 다시 운영자 화면에
```

**알림은 보내지 않습니다.** 운영자 화면을 직접 열어서 보는 방식입니다.
그래서 그 화면을 **폰 홈 화면이나 즐겨찾기에 걸어두는 걸 권합니다** —
답할 게 있으면 탭 제목이 `(2) 맘운자로 한마디` 로 보입니다.

사용자에게 **이메일·연락처를 묻지 않습니다.** 답장은 앱 화면 안에서만 오갑니다.

> 설정을 마치기 전까지는 앱이 글을 각자 폰에 보관해뒀다가, 끝나는 순간 자동으로
> 한꺼번에 보냅니다. 급하게 안 하셔도 글이 사라지지 않습니다.

---

## 1. Worker 코드 갱신 (1분)

1. Cloudflare → **Workers & Pages → maumjaro-ai → Edit code**
2. 에디터 내용을 전부 지우고 이 저장소의 `deepseek-worker.js`를 그대로 붙여넣기
3. **Save and deploy**

AI 맘운(기존 기능)은 그대로 돌아갑니다.

## 2. 대화 저장소 만들기 — KV (3분)

대화를 이어가려면 저장할 곳이 있어야 합니다.

1. Cloudflare → **Storage & Databases → KV → Create namespace**, 이름 `maumjaro-feedback`
2. Worker → **Settings → Bindings → Add → KV namespace**
   - Variable name: **`FEEDBACK_KV`** ← 이름이 정확해야 합니다
   - KV namespace: 방금 만든 `maumjaro-feedback`
3. 저장(자동 재배포)

무료 플랜으로 충분합니다(하루 읽기 10만 / 쓰기 1천).

## 3. 운영자 열쇠 정하기 (1분)

답장 화면에 들어갈 때 쓰는 비밀번호입니다. **아무나 추측 못 할 긴 문자열**로 정하세요.

Worker → **Settings → Variables and Secrets → Add** →
이름 `ADMIN_KEY`, 값은 정한 문자열, **Encrypt(Secret)** 로 저장.

유출된 것 같으면 값만 바꾸면 됩니다.

---

## 다 됐으면 — 운영자 화면 열기

```
https://maumjaro-ai.<서브도메인>.workers.dev/admin?key=정한열쇠
```

**이 주소를 폰 홈 화면에 추가해두세요.** 알림이 없으니 이게 창구 전부입니다.

- 최근 대화가 위로 옵니다. **"답장 차례"** 딱지가 붙은 것이 아직 답 안 한 대화입니다
- 맨 위에 `대화 N개 · 답장 차례 N개` 가 보이고, **탭 제목에도 `(N)` 이 붙습니다**
- 각 대화 아래 칸에 쓰고 **보내기** 를 누르면 그 사람 앱에 바로 붙습니다
- 대화마다 기기 정보(주사 횟수·사용 일수·기기 종류)가 아래 작게 보입니다
- 열어두면 1분마다 저절로 갱신됩니다 — 단, **답장을 쓰는 중에는 갱신하지 않습니다**
- 검색엔진에 잡히지 않도록 `noindex` 가 걸려 있습니다

## 잘 안 될 때

`/admin?key=...` 을 열면 **무엇이 빠졌는지 화면이 직접 알려줍니다.**
`ADMIN_KEY` 나 `FEEDBACK_KV` 가 없으면 그 이름과 고치는 위치를 띄웁니다.

| 증상 | 원인 |
|---|---|
| 화면에 "설정이 아직 끝나지 않았어요" | 2번이나 3번이 빠짐. 화면이 어느 쪽인지 알려줍니다 |
| "열쇠가 맞지 않습니다" | 주소의 `key=` 값이 `ADMIN_KEY` 와 다름 |
| 아무것도 안 뜸 ("아직 온 말이 없습니다") | 정상입니다. 아직 아무도 안 썼을 뿐입니다 |
| 앱에서 보냈는데 여기 없음 | 1번(Worker 갱신)이 빠짐. 글은 그 사람 폰에 있고 나중에 들어옵니다 |

## 나중에 알림이 필요해지면

지금은 알림 코드가 없습니다(일부러 뺐습니다 — 설정이 15분 줄었습니다).
카카오톡이나 디스코드로 받고 싶어지면 말씀해주세요. 카카오톡 "나에게 보내기"는
채널·알림톡과 달리 사업자등록과 검수가 필요 없어서, 15분이면 붙일 수 있습니다.

## 앱이 보내는 정보

화면에 적어둔 것이 전부입니다 — 쓴 글, 기기 종류(User-Agent), 화면 크기,
지금까지 주사를 놓은 횟수와 사용한 날짜 수. **이름·이메일·연락처는 받지 않습니다.**

사용자 식별은 기기에서 만든 128비트 난수(대화 ID) 하나로만 합니다.
이 ID를 아는 것 자체가 그 대화의 열쇠라 로그인 없이도 남의 대화를 볼 수 없습니다.
ID는 `maumjaro:feedbackThreadId` 에 저장되고, 기기 기록을 지우면 대화도 새로 시작됩니다.

사용자는 자기가 쓴 말을 `⋯` 로 **고치고 지울 수 있습니다.** 지우면 KV에서 본문이
실제로 없어지고, 운영자 화면에도 "지운 메시지"로 바뀝니다(자리는 남습니다 —
이미 답까지 한 말이 흔적 없이 사라지면 대화가 앞뒤로 안 맞습니다).
개발자 답장은 앱에서 건드릴 수 없습니다.

보내는 정보를 바꾸려면 `feedback.js`의 `meta()`와
화면 안내 문구(`.feedback-note`)를 **함께** 고쳐야 합니다.
