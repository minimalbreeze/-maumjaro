# 맘운자로 — 브랜드 원칙 (최상위, 절대 흔들리지 않음)

이 문서의 내용은 다른 모든 지시서·기능 요청보다 우선한다. 새 기능을 추가하거나
기존 기능을 바꿀 때, 아래 원칙과 충돌한다면 그 요청 쪽을 다시 확인하고 이 문서를
따른다.

## 브랜드 정체성

| 항목 | 내용 |
|---|---|
| 브랜드 | 맘운자로 |
| 핵심 서비스 | 오늘의 맘운 |
| 슬로건 | 오늘도 마음에 한 대 놓고 갑니다. 💉 |
| 핵심 UX | 운 → 마음 → 처방 → 주사 |
| 바이럴 루프 | 내 맘운 → 친구에게 주사 → 친구의 맘운 → 다시 공유 |

## 3대 최상위 원칙

1. **맘운자로를 운세 앱으로 전환하지 않는다.**
   운세/사주/토정비결은 부가 콘텐츠다. 앱을 여는 사람이 "운세 앱"으로 인식하게
   만드는 구조 변경(예: 운세를 첫 화면으로, 운세 탭을 홈보다 우선하는 내비게이션,
   운세 콘텐츠 비중이 마음/처방 경험을 압도하는 화면 구성)은 하지 않는다.

2. **오늘의 맘운이라는 핵심 경험 안에 운세를 편입한다.**
   운세는 독립된 목적지가 아니라 "오늘의 맘운"을 더 풍부하게 만드는 재료다.
   새 운세 기능을 만들 때는 그 콘텐츠가 최종적으로 오늘의 맘운(운+마음+처방)
   흐름과 어떻게 연결되는지를 먼저 설계한다. 연결 없이 운세 콘텐츠만 추가하지
   않는다.

3. **모든 주요 콘텐츠는 궁극적으로 마음의 주사 경험으로 연결한다.**
   어떤 기능(운세, AI 맘운, 친구 공유 등)이든 사용자의 여정이 "주사를 놓는다"는
   핵심 인터랙션으로 끝나거나 그 인터랙션을 향해 안내해야 한다. 주사 없이 정보만
   보여주고 끝나는 화면을 최종 목적지로 만들지 않는다.

## 핵심 UX 흐름을 지키는 방법

- 새 화면을 만들 때 "운 → 마음 → 처방 → 주사" 4단계 중 어디에 해당하는지
  분명히 한다. 이 4단계 밖에 있는 화면(예: 순수 정보 열람 화면)은 반드시
  이 흐름으로 돌아가는 CTA를 갖는다(기존 패턴: "그래서 오늘은? 💞",
  "처방 후보 보러가기 ›").
- 바이럴 루프(내 맘운 → 친구에게 주사 → 친구의 맘운 → 다시 공유)를 건드리는
  기능은 반드시 기존 DeviceMotion 주사 인터랙션(`Rx.wireExternalTrigger`)과
  기존 URL 공유 구조(LZString + 쿼리 파라미터)를 재사용한다. 새로 구현하지 않는다.
- 홈 탭이 앱의 기본 목적지다. 다른 탭(운세 포함)은 홈의 핵심 경험을 보조한다.

## 이 리포지토리의 기존 아키텍처 원칙 (브랜드 원칙과 함께 지킬 것)

- 정적 사이트, 빌드 도구 없음. GitHub Pages에 그대로 배포되며, 공식 주소는
  커스텀 도메인 `https://maumjaro.minimalbreeze.com/` 이다(저장소 루트의 `CNAME`
  파일로 연결). 기존 `minimalbreeze.github.io/-maumjaro/` 주소는 GitHub이 자동으로
  새 주소로 넘겨주므로 예전에 공유된 링크도 계속 동작한다. 절대 URL(og:url,
  og:image, canonical, sitemap.xml)은 항상 커스텀 도메인 기준으로 유지한다.
- `app.js`(마음 주사 핵심 로직)는 원칙적으로 무수정. 다른 파일이 재사용해야 할
  기능은 `window.MaumjaroCore` export를 통해서만 접근한다.
- `prescriptions.js`(처방 시스템)도 최소 수정. 외부 재사용은
  `window.MaumjaroRx` export를 통해서만 한다.
- 새 기능은 분리된 파일(`fortune.js`/`fortune-data.js`처럼)로 만들고, 기존
  파일에는 최소한의 추가 export만 더한다.
- localStorage 키는 전부 `maumjaro:` 접두사를 쓰고, 기존 키를 삭제하거나
  구조를 바꾸지 않는다. "모든 기록 초기화" 같은 사용자가 명시적으로 요청한
  삭제 동작 외에는 데이터를 지우지 않는다.
- 색상·아이콘·타이포그래피는 기존 팔레트(주사기 브랜드 컬러: 코랄~옐로
  그라디언트 `#ff9166`→`#ffc66b`, 딥틸 `#1f5c50`/`#2f6f5e`, 보라~핑크 CTA
  그라디언트 `#b779ef`→`#ff8fb3`)를 우선 재사용한다. 새 화면마다 새 팔레트를
  만들지 않는다.
- 작업 전 실제 코드를 읽고, 작업 후에는 변경 파일/신규 파일/변경 기능/기존
  기능에 미친 영향/테스트한 항목/발견된 문제/다음 단계를 보고한다. 문제를
  발견했을 때 구조를 크게 바꿔야 한다면 먼저 보고하고 승인을 받은 뒤 진행한다.

## 앱 아이콘

- 홈 화면 아이콘은 브랜드의 핵심 심볼인 "주사기"를 그대로 쓴다(`icon.svg`,
  코랄~옐로 그라디언트). 운세/사주 관련 심볼(크리스탈 볼 등)을 아이콘의
  주 모티프로 쓰지 않는다 — 이것도 "운세 앱으로 보이지 않기" 원칙의 연장이다.
- `icon.svg`(파비콘/Android any), `icon-180.png`(iOS 홈 화면),
  `icon-192.png`/`icon-512.png`(Android any), `icon-512-maskable.png`
  (Android adaptive icon, 안전 영역 확보된 버전) 네 가지를 함께 유지한다.
  아이콘을 바꿀 때는 네 파일을 모두 다시 만들어야 한다.
