# 맘운자로 페이스북 자동 홍보 — 설정 가이드

> 매일 아침 8시, 맘운자로 페이스북 페이지에 "오늘의 마음약"이 자동으로 올라갑니다.
> 코드는 이미 다 만들어져 있습니다. 아래 **① ~ ④만 하시면 됩니다.** 약 30분.
> 한 번 해두면 그 뒤로 손댈 일이 없습니다.

---

## 0. 먼저 알아둘 것 (중요)

페이스북은 자동 게시를 아무 데나 허용하지 않습니다.

| 대상 | 자동 게시 | 이유 |
|---|---|---|
| **페이스북 페이지** | ✅ 가능 | Meta가 공식 지원하는 유일한 통로 |
| 개인 프로필 타임라인 | ❌ 불가 | 2018년 4월 관련 API 폐지 |
| 페이스북 그룹 | ❌ 사실상 불가 | 2024년 Groups API 권한 대거 폐지 |

그래서 **"맘운자로 페이지"를 하나 만들고 거기에 올리는 것**이 우리가 할 수 있는 전부입니다.
개인 계정이나 그룹에 봇으로 뿌리는 건 우회하면 계정 정지 대상이라 하지 않습니다.

### 기대치도 미리 맞춰둡시다

팔로워 0인 새 페이지의 게시글은 **처음 몇 주 동안 거의 아무도 안 봅니다.** 페이스북
페이지의 자연 도달률은 팔로워 대비 1~5% 수준이라, 팔로워가 없으면 0입니다.
자동 게시는 "페이지를 살아있게 유지하는 것"이지 방문자를 만들어 주는 장치가 아닙니다.
초기 유입의 진짜 엔진은 **앱 안의 친구 공유 루프**입니다.

---

## 1. 전체 그림 (30초 요약)

카톡 스포츠뉴스 자동화와 구조가 같습니다. 부품만 바뀝니다.

- **콘텐츠 창고**: `game-data.js`의 마음약 46종 — 이미 있는 자산이라 문구가 안 떨어짐
- **심부름꾼**: GitHub Actions — 매일 정해진 시각에 알아서 깨어남 (무료, 서버 불필요)
- **배달부**: Meta Graph API — 페이지에 글을 올려줌

---

## ① 페이스북 페이지 만들기

1. `facebook.com/pages/create` 접속
2. 페이지 이름: `맘운자로`
3. 카테고리: `앱 페이지` 또는 `엔터테인먼트 웹사이트`
4. 소개: `마음에 놓는 오늘의 처방 💉 운세와 마음을 함께 봅니다.`
5. 프로필 사진은 리포의 `icon-512.png`, 커버 사진은 `og-image.jpg`를 쓰시면 됩니다.
6. 웹사이트 칸에 `https://maumjaro.minimalbreeze.com/` 를 넣어두세요.

---

## ② Meta 개발자 앱 만들기

1. `developers.facebook.com` 접속 → 우측 상단 **내 앱**
2. **앱 만들기** → 유형은 **비즈니스** 선택
3. 앱 이름: `맘운자로 자동게시` (아무거나 괜찮습니다)
4. 만들어진 앱에서 **설정 → 기본 설정**으로 가면 **앱 ID**와 **앱 시크릿**이 있습니다.
   잠시 뒤에 씁니다. (앱 시크릿은 '표시' 버튼을 눌러야 보입니다)

> 💡 **앱은 "개발 모드"로 그대로 두세요.**
> 내가 관리자인 내 페이지에, 내 토큰으로 올리는 것은 **앱 심사(App Review)가 필요 없습니다.**
> 많은 분이 여기서 괜히 심사를 신청했다가 몇 주씩 막힙니다. 신청하지 마세요.

---

## ③ 페이지 액세스 토큰 발급 (제일 까다로운 단계)

목표는 **만료되지 않는 페이지 토큰** 하나를 얻는 것입니다.

### 3-1. 짧은 토큰 받기

1. `developers.facebook.com/tools/explorer` (Graph API 탐색기) 접속
2. 오른쪽 위에서 방금 만든 앱을 선택
3. **권한 추가** 칸에서 아래 3개를 찾아 체크
   - `pages_manage_posts` (글 올리기)
   - `pages_read_engagement` (페이지 읽기)
   - `pages_show_list` (내 페이지 목록)
4. **사용자 액세스 토큰 생성** 클릭 → 로그인 → **맘운자로 페이지 선택** → 모두 허용
5. 칸에 생긴 긴 문자열이 **짧은 사용자 토큰**입니다. (1~2시간 뒤 만료됩니다)

### 3-2. 60일짜리로 바꾸기

브라우저 주소창에 아래를 넣고 이동합니다. 대괄호 부분을 본인 값으로 바꾸세요.

```
https://graph.facebook.com/v21.0/oauth/access_token?grant_type=fb_exchange_token&client_id=[앱ID]&client_secret=[앱시크릿]&fb_exchange_token=[3-1에서받은짧은토큰]
```

화면에 나오는 `access_token` 값이 **60일짜리 사용자 토큰**입니다.

### 3-3. 만료 없는 페이지 토큰 뽑기

역시 주소창에 넣습니다.

```
https://graph.facebook.com/v21.0/me/accounts?access_token=[3-2에서받은60일토큰]
```

결과에서 맘운자로 페이지 항목의 두 값을 챙기세요.

- `id` → 이게 **페이지 ID** 입니다
- `access_token` → 이게 **만료되지 않는 페이지 토큰** 입니다

> 💡 60일짜리 사용자 토큰으로 뽑은 **페이지** 토큰은 만료되지 않습니다.
> (비밀번호를 바꾸거나 권한을 취소하면 무효가 되니 그때만 다시 발급하면 됩니다.)

---

## ④ GitHub에 값 넣기

리포에서 **Settings → Secrets and variables → Actions → New repository secret**

| Name | Secret 값 |
|---|---|
| `FB_PAGE_ID` | ③에서 받은 페이지 `id` |
| `FB_PAGE_TOKEN` | ③에서 받은 페이지 `access_token` |

> ⚠️ **토큰은 누구에게도 채팅으로 보내지 마세요.** 딥시크 키 때와 같은 규칙입니다.
> 브라우저 화면에서 GitHub Secrets 칸으로 **직접** 옮기세요. 메모장을 거치셨다면
> 다 끝난 뒤 지우시고요.
> 혹시 어딘가 노출됐다면 Meta 앱 설정에서 **앱 시크릿을 재발급**하면 즉시 무효화됩니다.

---

## ⑤ 테스트하고 켜기

1. 리포의 **Actions** 탭 → 왼쪽에서 **페이스북 자동 홍보** 선택
2. 오른쪽 **Run workflow** 버튼 클릭
3. **`dry run` 체크를 켠 채로** 먼저 실행 → 로그에서 오늘 올라갈 문구를 확인 (실제 게시 안 함)
4. 문구가 마음에 들면, 다시 **Run workflow** → 이번엔 **`dry run` 체크를 끄고** 실행
5. 페이지에 글이 올라갔는지 확인

이후로는 **매일 아침 8시에 자동으로** 올라갑니다.

---

## 무슨 글이 올라가나요

마음약 46종을 하루 하나씩 소개합니다. 예:

```
💊 이런 약, 필요하신 분?

『셀카보정정』

사진이 아니라 자신감을 보정한다.

운세도 보고, 마음에 주사도 한 대 놓고 가세요 💉
https://maumjaro.minimalbreeze.com/?utm_source=facebook&...

#맘운자로 #마음처방 #오늘의운세 #셀카보정정 #연애
```

- **같은 약이 이틀 연속 나오지 않습니다.** 가장 짧은 재등장 간격이 5일입니다.
- **제철에만 나오는 약이 있습니다.** 월요병긴급주사는 월요일에만, 새해다짐보존제는
  1월에만 나옵니다. 7월에 크리스마스 약이 올라가는 일은 없습니다.
- 도입부·마무리 문구가 날짜에 따라 4가지로 바뀝니다.
- 링크에 `utm_source=facebook`이 붙어 있어서 **GA4에서 페이스북 유입만 따로** 볼 수 있습니다.

문구를 바꾸고 싶으시면 `scripts/fb-content.mjs`의 `OPENERS` / `CLOSERS`만 고치면 됩니다.
마음약을 늘리면 홍보 문구도 자동으로 늘어납니다.

---

## 문제가 생겼을 때

Actions 탭에서 실패한 실행을 눌러 로그를 보시면 원인이 한국어로 찍혀 있습니다.

| 로그에 보이는 것 | 뜻과 해결 |
|---|---|
| `⏸️ 아직 페이스북 연결 전입니다` | Secret 두 개가 아직 없습니다. ④를 하세요. (실패 아님) |
| `code 190` | 토큰이 만료·무효입니다. ③을 다시 해서 `FB_PAGE_TOKEN`을 갱신하세요 |
| `code 200` | 권한 부족. ③-1의 권한 3개가 빠졌는지 확인하세요 |
| `code 100` | `FB_PAGE_ID`가 잘못됐습니다. 개인 계정 ID가 아니라 **페이지** ID여야 합니다 |
| 아무 일도 안 일어남 | 리포가 60일 이상 조용하면 GitHub이 예약 실행을 자동으로 끕니다. Actions 탭에서 다시 켜면 됩니다 |

> 예약 실행은 혼잡할 때 5~15분 늦게 도는 경우가 있습니다. 정상입니다.

---

## 비용

**0원입니다.** GitHub Actions는 public 리포에서 무제한 무료이고,
Meta Graph API도 이 정도 사용량은 무료입니다.
